{
    "*.ts": [
        "tslint -p tsconfig.json --fix",
        "prettier --write",
        "tslint -p tsconfig.json -c tslint.json",
        "git add"
    ],
    "*.{js,json,md,yml,yaml}": [
        "prettier --write",
        "git add"
    ]
}
